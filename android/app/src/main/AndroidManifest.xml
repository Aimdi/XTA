<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aimdi.xta"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Force usage of the file picker library even on SDKs < 19, as we handle it at runtime -->
    <uses-sdk tools:overrideLibrary="com.kineapps.flutter_file_dialog" />

    <!-- Only for importing and exporting on older versions of Android. From 11
         on, files are reached through the system picker (SAF), which needs no
         permission at all — WRITE is ignored there and READ was replaced in 13.
         MANAGE_EXTERNAL_STORAGE used to be declared "for Android 11 and above",
         but nothing ever requested it (it needs a Settings flow this app does
         not have), so it granted nothing and only put "all files access" on the
         permission list of an app that reads a timeline. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- io.flutter.app.FlutterApplication is an android.app.Application that
         calls FlutterMain.startInitialization(this); in its onCreate method.
         In most cases you can leave this as-is, but you if you want to provide
         additional functionality it is fine to subclass or reimplement
         FlutterApplication and put your custom class here. -->
    <!-- Backups are disabled so the database holding X session tokens can
         never leave the device via adb or cloud backups -->
    <application
        android:name="com.aimdi.xta.XtaApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:theme="@android:style/Theme.Black.NoTitleBar"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@android:style/Theme.Black.NoTitleBar" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="x.com"
                    android:scheme="http" />

                <data
                    android:host="x.com"
                    android:scheme="https" />

                <data
                    android:host="twitter.com"
                    android:scheme="http" />

                <data
                    android:host="twitter.com"
                    android:scheme="https" />

                <data
                    android:host="mobile.twitter.com"
                    android:scheme="http" />

                <data
                    android:host="mobile.twitter.com"
                    android:scheme="https" />

                <data
                    android:host="www.twitter.com"
                    android:scheme="http" />

                <data
                    android:host="www.twitter.com"
                    android:scheme="https" />

                <data
                    android:host="t.co"
                    android:scheme="http" />

                <data
                    android:host="t.co"
                    android:scheme="https" />

                <!-- Discord embedding websites -->
                <data
                    android:host="fxtwitter.com"
                    android:scheme="https" />

                <data
                    android:host="vxtwitter.com"
                    android:scheme="https" />

                <data
                    android:host="*vxtwitter.com"
                    android:scheme="https" />
                
                <data
                    android:host="fixupx.com"
                    android:scheme="http" />

                <data
                    android:host="fixupx.com"
                    android:scheme="https" />

                <data
                    android:host="*fixupx.com"
                    android:scheme="https" />

            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Keeps read-aloud and podcast playback alive when the app leaves the
             foreground, and puts their controls on the lockscreen. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!-- Android 11+ hides third-party TTS engines unless we declare this.
             Without it, getEngines() cannot see Sherpa / Next-gen Kaldi, setEngine
             cannot bind it, and Vorlesen stays silent even when it is the system
             preferred module. -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <package android:name="com.k2fsa.sherpa.onnx.tts.engine" />
        <package android:name="org.woheller69.ttsengine" />
    </queries>
</manifest>
