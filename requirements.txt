# Pinned exactly for reproducible builds: generate_icons.py builds the launcher
# icons through Pillow, and its PNG output can change between versions. See
# docs/reproducible-builds.md.
pillow==12.3.0
requests==2.34.2
