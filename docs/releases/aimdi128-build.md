# aimdi128 release build

User-authorized release of merged PRs #256, #257 and #258.

Build source: fb5eb74309390c211e3885096006bf076f6c25cb (branch aimdi128).
Use the existing attach-release-apks workflow to build signed release APKs and
publish the aimdi128 tag at that exact source commit. This trigger document is
not included in the release source. Close the trigger PR after verification.
