# aimdi134 icon and release

Use the supplied white orbital mark on black for XTA's launcher and in-app
branding. Crop the photograph's empty margins without stretching the mark.
Size the legacy icon to fill its frame, and place adaptive/themed artwork in
Android's safe area without a second tooling inset. Preserve smooth edges.

Release the merged reader recovery and seven safety improvements from PR #275.
Advance the Android base version code beyond all aimdi133 ABI variants, retain
the app id and signing certificate, and publish all four signed APK variants
with source/signature verification and checksums using the existing release
workflow. Verify the new icon visually and run the repository release gates.
